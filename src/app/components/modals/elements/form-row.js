import Ember from 'ember';

export default Ember.Component.extend({
  classNames: ['modal-row', 'modal-row-main'],

  /**
   * To inject
   * @required
   * @type string
   */
  translationBase: null
});
