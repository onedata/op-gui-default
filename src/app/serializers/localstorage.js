/**
 * A localstorage serializer. Currently not used.
 * @todo decide if this should be removed
 */

import { LSSerializer } from 'ember-localstorage-adapter';

export default LSSerializer.extend({});
