{{#if isLoaded}}
  {{yield}}
{{else}}
  {{#if isLoading}}
    {{spin-spinner-block sizeClass="md" class=_spinnerBlockClass}}
  {{else if isError}}
    {{resource-load-error message=customErrorMessage reason=errorReason}}
  {{/if}}
{{/if}}
