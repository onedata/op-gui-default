{
  "scripts": {
    "babel": "rm -rf locales locales.orig && cp -Lr ../src/app/locales locales.orig && babel --presets 'es2015' --out-dir locales locales.orig",
    "print": "rm -f flatted.txt && node locale-flatten.js > flatted.txt"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "flat": "^2.0.1"
  }
}
